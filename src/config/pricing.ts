// Pricing configuration flags
// Có thể bật/tắt VAT hoặc đổi tỷ lệ tại đây thay vì hard-code trong logic.
export const VAT_ENABLED = true; // bật thuế giá trị gia tăng
export const VAT_RATE = 0.10; // 10%
