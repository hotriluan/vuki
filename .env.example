# Public base URL of the deployed site (used for metadata + canonical URLs)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Example: future API base (not yet used)
# NEXT_PUBLIC_API_BASE=https://api.your-domain.com

# (Server-side secret examples, do NOT prefix with NEXT_PUBLIC_ and do NOT commit real values)
# STRIPE_SECRET_KEY=sk_live_xxx

# Primary database (MySQL). Example remote:
# DATABASE_URL=mysql://root:password@192.168.18.33:3306/vuki
# Local dev (Docker):
# DATABASE_URL=mysql://root:root@127.0.0.1:3306/vuki

# Admin secrets (shared-secret auth for admin endpoints)
# ADMIN_SECRET=change-me
# ADMIN_REBUILD_SEARCH_SECRET=change-me-override

# Logging level (pino): trace | debug | info | warn | error | fatal
# LOG_LEVEL=info

# Public rate limiting (search & orders)
# PUBLIC_RATE_LIMIT=60
# PUBLIC_RATE_WINDOW_MS=300000

# Admin rebuild rate limiting
# ADMIN_REBUILD_LIMIT=5
# ADMIN_REBUILD_WINDOW_MS=300000

# Feature flags (use '1' to enable, omit or '0' to disable)
# NEXT_PUBLIC_FEATURE_DARK_MODE=1
